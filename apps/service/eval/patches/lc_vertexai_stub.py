# ragas references langchain_community.chat_models.vertexai.ChatVertexAI only in an
# isinstance check (MULTIPLE_COMPLETION_SUPPORTED list). The class was removed from
# langchain-community 0.4.x. This stub keeps the import chain intact; the class will
# never match any LLM instance we actually use.
class ChatVertexAI:
    pass
